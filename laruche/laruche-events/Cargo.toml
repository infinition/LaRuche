[package]
name = "laruche-events"
version.workspace = true
edition = "2021"
license = "MPL-2.0"
description = "Bus d'événements structuré (NDJSON) pour LaRuche - observabilité / audit / contrôle."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

