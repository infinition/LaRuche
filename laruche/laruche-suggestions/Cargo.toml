[package]
name = "laruche-suggestions"
version = "0.2.0"
edition = "2021"
license = "MPL-2.0"
description = "Moteur d'autocomplete/suggestions unifié (fichiers, nœuds mémoire, outils, agents, commandes) pour LaRuche."

[dependencies]
serde = { version = "1", features = ["derive"] }

# Crate standalone tant que les autres pistes tournent. À intégrer au workspace (retirer ce bloc) en passe finale.
