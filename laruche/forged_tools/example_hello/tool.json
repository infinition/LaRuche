{
  "name": "example_hello",
  "description": "Reference Forged Tool. Greets someone so you can see the pipeline end to end: schema validation, slot substitution, script execution, and the observation handed back to the agent. Copy this folder to start your own.",
  "parameters": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Who to greet."
      },
      "language": {
        "type": "string",
        "description": "Greeting language.",
        "enum": ["fr", "en"],
        "default": "fr"
      }
    },
    "required": ["name"]
  },
  "command": "python \"{{forged_tool_dir}}/run.py\" \"{{name}}\" \"{{language}}\"",
  "danger": "safe",
  "timeout_secs": 10
}
