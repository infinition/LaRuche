[package]
name = "laruche-permissions"
version = "0.1.0"
edition = "2021"
license = "MPL-2.0"
description = "Moteur de permissions autonome pour LaRuche."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

# Crate standalone (detachee du workspace parent) jusqu'a la passe d'integration.
