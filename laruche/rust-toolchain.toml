[toolchain]
# Host-agnostic: resolves to the runner's own stable toolchain.
# Pinning a host triple here (e.g. stable-x86_64-pc-windows-msvc) breaks
# every non-Windows build, CI included.
channel = "stable"
