[package]
name = "laruche-compaction"
version.workspace = true
edition = "2021"
license = "MPL-2.0"
description = "Compaction de conversations et stockage des gros resultats d'outils pour LaRuche."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"

# Crate standalone (detachee du workspace parent) jusqu'a la passe d'integration.
